// haolamnm

#include <bits/stdc++.h>
using namespace std;
 
int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
 
    unsigned int l, r;
    cin >> l >> r;
    for (int i=l; i<=r; i++) {
        cout << i << " ";
    }
 
    return 0;
}